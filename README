Fake SMTP server to relay any message to a specific mail account via a
specific MTA.

Usage:

  $ perl resmtp.pl [-d|--daemon] [-h|--host <listen_host>] [-p|--port <listen_port>] <recipient@example.net> [<smtp_host[:port]>]

